[project]
name = "conductor-mcp"
version = "0.1.0"
description = "MCP server sidecar for Conductor — exposes orchestrator tools to any MCP client"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]

[project.scripts]
conductor-mcp = "conductor_mcp.server:main"
